FROM scratch

#RUN git clone  --depth 1 --single-branch  --branch master https://github.com/pontus-vision/pontus-odb-graph.git


COPY   conf /pontus-odb-graph/conf
COPY   config /pontus-odb-graph/config
COPY   j2 /pontus-odb-graph/j2
COPY   bin /pontus-odb-graph/bin


